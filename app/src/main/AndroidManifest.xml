<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.yoga">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".Program"></activity>
        <activity android:name=".Depression" />
        <activity android:name=".Calm" />
        <activity android:name=".Relaxation" />
        <activity android:name=".BeginnerMediation" />
        <activity android:name=".Alarm_man" />
        <activity android:name=".Music" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name=".AlarmReceiver" />

        <activity android:name=".Contributors" />
        <activity android:name=".Blog" />
        <activity android:name=".AboutUs" />
        <activity android:name=".Profile" />
        <activity android:name=".Beginner" />
        <activity android:name=".Tabs" />
        <activity

            android:name=".Login"
            android:label="@string/login" />
        <activity
            android:name=".Register"
            android:label="@string/register" />
        <activity android:name=".Begin" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>